main: me.antritus.astraldupe.AstralDupe
loader: me.antritus.astraldupe.AstralDupeLoader
name: AstralDupe
version: '1.1'
api-version: '1.20'
prefix: AstralDupe
authors:
  - Antritus (AstralDupe)
  - Prorickey (ClassicDupe)
  - ModdedKid (ClassicDupe)
  - BraveDishDealer (ClassicDupe)
description: Dupe core of astral dupe. This uses ClassicDupe plugin in the internals.



dependencies:
  server:
    LuckPerms:
      load: BEFORE
      required: true
      join-classpath: true
    PlaceholderAPI:
      load: BEFORE
      required: true
    spark:
      load: BEFORE
      required: true
    CosmicCapital:
      load: BEFORE
      required: false
    Wormhole:
      load: BEFORE
      required: false
    Fluffy:
      load: BEFORE
      required: true
    AntsFactions:
      load: BEFORE
      required: false
